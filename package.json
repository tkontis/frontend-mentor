{
  "name": "frontend-mentor-assignments",
  "private": true,
  "workspaces": ["recipe-page"],
  "scripts": {
    "install": "npm install",
    "build": "npm run build --workspaces",

    "format": "prettier --write .",
    "format-check": "prettier --check ."
  },
  "devDependencies": {


    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3"
  }
}
