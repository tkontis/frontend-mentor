import { FC } from 'react';
import styles from './Product.module.css';

const Product: FC = () => {
    return <main className={styles.productContainer}>Main Product Content Goes here</main>;
};

export default Product;
